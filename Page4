package com.example.finalproject;

import android.annotation.SuppressLint;
import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.RadioButton;
import android.widget.TextView;
import android.widget.Toast;

import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;

public class MainActivity4 extends AppCompatActivity {
    TextView t1,t2;
    EditText e1,e2;
    Button b1,b2,b3,b4,b5;
    RadioButton r1,r2,r3,r4,r5,r6,r7,r8;
    Double a,b,c;
    Double e,f,g,h,i;
    int r=0,s=0;

    @SuppressLint("MissingInflatedId")
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_main4);
        ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.main), (v, insets) -> {
            Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());
            v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);
            return insets;
        });
        e1=findViewById(R.id.editTextNumberDecimal5);
        e2=findViewById(R.id.editTextNumberDecimal6);
        t1=findViewById(R.id.textView8);
        t2=findViewById(R.id.textView150);
        b1=findViewById(R.id.button7);
        b2=findViewById(R.id.button8);
        b3=findViewById(R.id.button10);
        b4=findViewById(R.id.button13);
        b5=findViewById(R.id.button58);
        r1=findViewById(R.id.radioButton);
        r2=findViewById(R.id.radioButton2);
        r3=findViewById(R.id.radioButton3);
        r4=findViewById(R.id.radioButton4);
        r5=findViewById(R.id.radioButton5);
        r6=findViewById(R.id.radioButton6);
        r7=findViewById(R.id.radioButton7);
        r8=findViewById(R.id.radioButton8);
        a = getIntent().getDoubleExtra("Value_a", 0);
        b = getIntent().getDoubleExtra("Value_b", 0);
        r1.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                c=a;
                String s1 = e1.getText().toString().trim();
                String s2 = e2.getText().toString().trim();

                e = s1.isEmpty() ? 0.0 : Double.parseDouble(s1);
                f = s2.isEmpty() ? 0.0 : Double.parseDouble(s2);
                if(e==0.0){
                    Toast.makeText(MainActivity4.this, "Enter Weight", Toast.LENGTH_SHORT).show();
                    r=0;
                }

                c=(c/10)*0.8;
                i=e*0.03;
                g=(e-i)-f;
                h=c*g;
                r=(int) Math.ceil(h);
                r8.setChecked(false);
                r2.setChecked(false);
                r3.setChecked(false);
                r4.setChecked(false);
                r5.setChecked(false);
                r6.setChecked(false);
                r7.setChecked(false);
            }
        });
        r2.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                c=a;
                String s1 = e1.getText().toString().trim();
                String s2 = e2.getText().toString().trim();

                e = s1.isEmpty() ? 0.0 : Double.parseDouble(s1);
                f = s2.isEmpty() ? 0.0 : Double.parseDouble(s2);
                if(e==0.0){
                    Toast.makeText(MainActivity4.this, "Enter Weight", Toast.LENGTH_SHORT).show();
                    r=0;
                }

                c=(c/10)*0.99;
                g=e-f;
                h=c*g;
                r=(int) Math.ceil(h);
                r8.setChecked(false);
                r1.setChecked(false);
                r3.setChecked(false);
                r4.setChecked(false);
                r5.setChecked(false);
                r6.setChecked(false);
                r7.setChecked(false);
            }
        });
        r3.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                c=a;
                String s1 = e1.getText().toString().trim();
                String s2 = e2.getText().toString().trim();

                e = s1.isEmpty() ? 0.0 : Double.parseDouble(s1);
                f = s2.isEmpty() ? 0.0 : Double.parseDouble(s2);
                if(e==0.0){
                    Toast.makeText(MainActivity4.this, "Enter Weight", Toast.LENGTH_SHORT).show();
                    r=0;
                }

                c=(c/10)*0.72;
                i=e*0.03;
                g=(e-i)-f;
                h=c*g;
                r=(int) Math.ceil(h);
                r8.setChecked(false);
                r1.setChecked(false);
                r2.setChecked(false);
                r4.setChecked(false);
                r5.setChecked(false);
                r6.setChecked(false);
                r7.setChecked(false);
            }
        });
        r4.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                c=a;
                String s1 = e1.getText().toString().trim();
                String s2 = e2.getText().toString().trim();

                e = s1.isEmpty() ? 0.0 : Double.parseDouble(s1);
                f = s2.isEmpty() ? 0.0 : Double.parseDouble(s2);
                if(e==0.0){
                    Toast.makeText(MainActivity4.this, "Enter Weight", Toast.LENGTH_SHORT).show();
                    r=0;
                }

                c=(c/10)*0.89;
                g=e-f;
                h=c*g;
                r=(int) Math.ceil(h);
                r8.setChecked(false);
                r1.setChecked(false);
                r2.setChecked(false);
                r3.setChecked(false);
                r5.setChecked(false);
                r6.setChecked(false);
                r7.setChecked(false);
            }
        });
        r5.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                c=a;
                String s1 = e1.getText().toString().trim();
                String s2 = e2.getText().toString().trim();

                e = s1.isEmpty() ? 0.0 : Double.parseDouble(s1);
                f = s2.isEmpty() ? 0.0 : Double.parseDouble(s2);
                if(e==0.0){
                    Toast.makeText(MainActivity4.this, "Enter Weight", Toast.LENGTH_SHORT).show();
                    r=0;
                }

                c=(c/10)*0.89;
                i=e*0.03;
                g=(e-i)-f;
                h=c*g;
                r=(int) Math.ceil(h);
                r8.setChecked(false);
                r1.setChecked(false);
                r2.setChecked(false);
                r3.setChecked(false);
                r4.setChecked(false);
                r6.setChecked(false);
                r7.setChecked(false);
            }
        });
        r6.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                c=a;
                String s1 = e1.getText().toString().trim();
                String s2 = e2.getText().toString().trim();

                e = s1.isEmpty() ? 0.0 : Double.parseDouble(s1);
                f = s2.isEmpty() ? 0.0 : Double.parseDouble(s2);
                if(e==0.0){
                    Toast.makeText(MainActivity4.this, "Enter Weight", Toast.LENGTH_SHORT).show();
                    r=0;
                }

                c=(c/10)*0.85;
                g=e-f;
                h=c*g;
                r=(int) Math.ceil(h);
                r8.setChecked(false);
                r1.setChecked(false);
                r2.setChecked(false);
                r3.setChecked(false);
                r4.setChecked(false);
                r5.setChecked(false);
                r7.setChecked(false);
            }
        });
        r7.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                c=a;
                String s1 = e1.getText().toString().trim();
                String s2 = e2.getText().toString().trim();

                e = s1.isEmpty() ? 0.0 : Double.parseDouble(s1);
                f = s2.isEmpty() ? 0.0 : Double.parseDouble(s2);
                if(e==0.0){
                    Toast.makeText(MainActivity4.this, "Enter Weight", Toast.LENGTH_SHORT).show();
                    r=0;
                }

                c=(c/10)*0.85;
                i=e*0.03;
                g=(e-i)-f;
                h=c*g;
                r=(int) Math.ceil(h);
                r8.setChecked(false);
                r1.setChecked(false);
                r2.setChecked(false);
                r3.setChecked(false);
                r4.setChecked(false);
                r5.setChecked(false);
                r6.setChecked(false);
            }
        });
        r8.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                c=a;
                String s1 = e1.getText().toString().trim();
                String s2 = e2.getText().toString().trim();

                e = s1.isEmpty() ? 0.0 : Double.parseDouble(s1);
                f = s2.isEmpty() ? 0.0 : Double.parseDouble(s2);
                if(e==0.0){
                    Toast.makeText(MainActivity4.this, "Enter Weight", Toast.LENGTH_SHORT).show();
                    r=0;
                }

                c=(c/10)*0.62;
                g=e-f;
                h=c*g;
                r=(int) Math.ceil(h);
                r1.setChecked(false);
                r2.setChecked(false);
                r3.setChecked(false);
                r4.setChecked(false);
                r5.setChecked(false);
                r6.setChecked(false);
                r7.setChecked(false);
            }
        });
        b1.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                if (!r1.isChecked() && !r2.isChecked() && !r3.isChecked() && !r4.isChecked() && !r5.isChecked() && !r6.isChecked() && !r7.isChecked() && !r8.isChecked()){
                    Toast.makeText(MainActivity4.this, "Select one option", Toast.LENGTH_SHORT).show();
                    if(e==0.0){
                        Toast.makeText(MainActivity4.this, "Enter Weight", Toast.LENGTH_SHORT).show();
                        r=0;
                    }
                }
                t1.setText(String.valueOf(r));
                s=(int) Math.ceil(r*0.99);
                t2.setText(String.valueOf(s));
            }
        });
        b2.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                a = getIntent().getDoubleExtra("Value_a", 0);
                b = getIntent().getDoubleExtra("Value_b", 0);
                Intent in =new Intent(MainActivity4.this, MainActivity6.class);
                in.putExtra("Value_a",a);
                in.putExtra("Value_b",b);
                startActivity(in);

            }
        });
        b3.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                a = getIntent().getDoubleExtra("Value_a", 0);
                b = getIntent().getDoubleExtra("Value_b", 0);
                Intent in =new Intent(MainActivity4.this, MainActivity5.class);
                in.putExtra("Value_a",a);
                in.putExtra("Value_b",b);
                startActivity(in);
            }
        });
        b4.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                a = getIntent().getDoubleExtra("Value_a", 0);
                b = getIntent().getDoubleExtra("Value_b", 0);
                r1.setChecked(false);
                r2.setChecked(false);
                r3.setChecked(false);
                r4.setChecked(false);
                r5.setChecked(false);
                r6.setChecked(false);
                r7.setChecked(false);
                r8.setChecked(false);
                e1.setText("");
                e2.setText("");
                t1.setText("");
                t2.setText("");
            }
        });
        b5.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                a = getIntent().getDoubleExtra("Value_a", 0);
                b = getIntent().getDoubleExtra("Value_b", 0);
                Intent in =new Intent(MainActivity4.this, MainActivity3.class);
                in.putExtra("Value_a",a);
                in.putExtra("Value_b",b);
                startActivity(in);
            }
        });
    }
}
